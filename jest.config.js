/** @type {import('ts-jest').JestConfigWithTsJest} */
// eslint-disable-next-line no-undef
module.exports = {
  roots: [
    "<rootDir>/src"
  ],
  preset         : 'ts-jest',
  testEnvironment: 'node',
}